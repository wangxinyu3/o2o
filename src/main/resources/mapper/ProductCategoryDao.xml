<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.ProductCategoryDao">
    <select id="queryProductCategoryList" resultType="entity.ProductCategory"
            parameterType="long">
        select
        product_category_id,
        product_category_name,
        priority,
        create_time,
        shop_id
        from
        tb_product_category
        where
        shop_id = #{shopId}
        order by
        priority DESC
    </select>
</mapper>